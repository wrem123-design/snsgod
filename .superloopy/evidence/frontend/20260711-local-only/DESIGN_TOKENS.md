# Local-only settings token reuse

- Reused the existing settings card, section tab, help text, and SwitchLine tokens from `DESIGN.md` and `SettingsScreen`.
- No new raw color, gradient, shadow, radius, type scale, or decorative icon token was introduced.
- The data-boundary card precedes provider and Oracle details so the network boundary is read before credentials or endpoints.
- The switch retains the established minimum touch target and Android accessibility label/value behavior.

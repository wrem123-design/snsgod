# Oracle resync design-token contract

This fix reuses the repository `DESIGN.md` contract without adding visual values.

- Surface: `app.panel`
- Feedback surface: existing `statusBox`, using the established warm accent treatment
- Text: `app.text` and `app.sub`
- Border: `app.border`
- Accent: `app.accent` and `app.accentText`
- Shape: `control.round`
- Spacing: `card.padding` and `compact.gap`
- Touch target: `control.touch`

No raw color, spacing, radius, shadow, typography, or motion value was introduced.

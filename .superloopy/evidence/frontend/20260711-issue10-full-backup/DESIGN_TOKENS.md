# Full-backup settings token record

Source contract: `DESIGN.md`

- Direction: warm, compact, character-first local messenger utility
- Surface: existing `styles.card` backup card
- Hierarchy: existing `styles.cardTitle`, `styles.label`, and `styles.help`
- Actions: existing `styles.secondary`, `styles.secondaryText`, and disabled treatment
- Font: native system sans for Korean legibility and offline rendering

The change reuses the established Settings card, typography, spacing, border, radius, pressed, and disabled tokens. No new color, raw spacing value, font, shadow, gradient, or motion was introduced.
